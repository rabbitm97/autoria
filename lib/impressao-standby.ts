// IMP-STANDBY (FASE 4, 08/set/2026): valores de impressão em
// calibração — NENHUM preço de impressão aparece (público ou logado)
// enquanto true. Pedidos: anônimo via e-mail; logado via thread de
// suporte. Religar no lançamento = false (e nada mais).
export const IMPRESSAO_STANDBY: boolean = true;

export const CONTATO_IMPRESSAO_EMAIL = "contato@useautoria.com";

// Mensagem única das rotas gateadas (server-side).
export const MSG_STANDBY =
  "Os orçamentos de impressão estão em calibração. Fale com a equipe: contato@useautoria.com";
